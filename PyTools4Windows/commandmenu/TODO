

* add ability for sequential user input strings

* add ability to prompt list editing via menucmd



X* Add json support... %100  DONE

X* Add mode switching

X* Add command history

X* Add comments/function docs

X* Is it possible to run the compiler from the PATH?

   YES!


PATH --> Compiler(mode, json, meta)
 ref  ^      func w/ args               ~dependency and reference of
  ^   :                 ^
  | <.:.dependency~...> |
  |   V              $  |
venv  --> (Compiler -> "*.json" "meta.json")           $contains refs in the code
 env        script has paths
  |                     |
  |   <...............> |
  V            :        V
   dir         V         files                       files
Program Files --> (Compiler, json, meta)   <==>   Compiler  <--> json
     |     subfolder  |                             ^
     |                | evaluation                  | same folder
     V                V                             V
PATH --> Compiler(json, meta) mode                 meta




